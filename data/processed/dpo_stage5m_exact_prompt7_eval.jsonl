{"prompt": "为什么不能只看 loss 判断一次 SFT 是否成功？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "只能先说公开数据跑通，但不能只凭 loss 判断项目是否通过。本项目 public-SFT 指用公开通用 instruction 数据教 LoRA/SFT to do the task, so it can still fail if the model doesn't learn the target concept. 我们还专门记录了相关误区：只看 loss 不够，必须看哪些训练分布、何时开始做 custom-SFT 和为什么失败不丢脸。", "source_type": "stage5m_prompt7_actual_v6_failure"}
{"prompt": "为什么不能只看 loss 判断一次 SFT 是否成功？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "DPO preference accuracy 到 1.0 就说明模型行为已经通过，prompt 失败只是偶然。", "source_type": "stage5m_prompt7_generic_3"}
{"prompt": "为什么不能只看 loss 判断一次 SFT 是否成功？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "eval loss 比 train loss 更客观，所以 eval loss 好看就可以不用再看具体输出。", "source_type": "stage5m_prompt7_generic_2"}
{"prompt": "为什么不能只看 loss 判断一次 SFT 是否成功？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "只要 loss 降低，就说明 SFT 已经成功。固定 prompt 只是展示用，不需要作为验收标准。", "source_type": "stage5m_prompt7_generic_1"}
{"prompt": "train loss 降了，为什么还不能说 SFT 成功？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "eval loss 比 train loss 更客观，所以 eval loss 好看就可以不用再看具体输出。", "source_type": "stage5m_prompt7_generic_2"}
{"prompt": "train loss 降了，为什么还不能说 SFT 成功？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "只要 loss 降低，就说明 SFT 已经成功。固定 prompt 只是展示用，不需要作为验收标准。", "source_type": "stage5m_prompt7_generic_1"}
{"prompt": "train loss 降了，为什么还不能说 SFT 成功？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "loss 是训练目标上的平均损失，不是固定信号是否通过的最终验收标准。本项目 public-SFT 能正常下降 2.5%，但 DPO 发现它没有修正 LoRA/SFT/DPO 的技术概念误解；所以要继续做三方验收，并在 public-SFT 上保持 loss 平稳下降。", "source_type": "stage5m_prompt7_actual_stage5k_failure"}
{"prompt": "train loss 降了，为什么还不能说 SFT 成功？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "loss 完全没有意义，只能凭人工感觉判断训练是否成功。", "source_type": "stage5m_prompt7_generic_5"}
{"prompt": "eval loss 看起来不错，为什么还要做固定 prompt 对比？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "只要 loss 降低，就说明 SFT 已经成功。固定 prompt 只是展示用，不需要作为验收标准。", "source_type": "stage5m_prompt7_generic_1"}
{"prompt": "eval loss 看起来不错，为什么还要做固定 prompt 对比？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "loss 是训练目标上的平均损失，不是固定信号是否通过的最终验收标准。本项目 public-SFT 能正常下降 2.5%，但 DPO 发现它没有修正 LoRA/SFT/DPO 的技术概念误解；所以要继续做三方验收，并在 public-SFT 上保持 loss 平稳下降。", "source_type": "stage5m_prompt7_actual_stage5k_failure"}
{"prompt": "eval loss 看起来不错，为什么还要做固定 prompt 对比？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "loss 完全没有意义，只能凭人工感觉判断训练是否成功。", "source_type": "stage5m_prompt7_generic_5"}
{"prompt": "eval loss 看起来不错，为什么还要做固定 prompt 对比？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "只能先说它失败了，但不能只凭 loss 判断它是不是朝着目标行为靠近。本项目在 public-SFT 上用固定 prompt 做三方对比后发现它仍然没有解决目标行为偏好的问题。因此 Stage 4A 需要做两件事：1. 安排人工监督数据；2. 找到目标行为偏好的方法并记录在 DPO 文件里。", "source_type": "stage5m_prompt7_actual_v7_failure"}
{"prompt": "eval loss 看起来不错，为什么还要做固定 prompt 对比？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "只要新的 loss-vs-behavior prompt 答对，旧的 LoRA/SFT/DPO prompt 回归也可以忽略。", "source_type": "stage5m_prompt7_generic_4"}
{"prompt": "为什么 DPO v6 preference eval 很好看，仍然不能说行为通过？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "loss 是训练目标上的平均损失，不是固定信号是否通过的最终验收标准。本项目 public-SFT 能正常下降 2.5%，但 DPO 发现它没有修正 LoRA/SFT/DPO 的技术概念误解；所以要继续做三方验收，并在 public-SFT 上保持 loss 平稳下降。", "source_type": "stage5m_prompt7_actual_stage5k_failure"}
{"prompt": "为什么 DPO v6 preference eval 很好看，仍然不能说行为通过？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "loss 完全没有意义，只能凭人工感觉判断训练是否成功。", "source_type": "stage5m_prompt7_generic_5"}
{"prompt": "为什么 DPO v6 preference eval 很好看，仍然不能说行为通过？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "只能先说它失败了，但不能只凭 loss 判断它是不是朝着目标行为靠近。本项目在 public-SFT 上用固定 prompt 做三方对比后发现它仍然没有解决目标行为偏好的问题。因此 Stage 4A 需要做两件事：1. 安排人工监督数据；2. 找到目标行为偏好的方法并记录在 DPO 文件里。", "source_type": "stage5m_prompt7_actual_v7_failure"}
{"prompt": "为什么 DPO v6 preference eval 很好看，仍然不能说行为通过？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "只要新的 loss-vs-behavior prompt 答对，旧的 LoRA/SFT/DPO prompt 回归也可以忽略。", "source_type": "stage5m_prompt7_generic_4"}
{"prompt": "为什么 DPO v6 preference eval 很好看，仍然不能说行为通过？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "只能先说公开数据跑通，但不能只凭 loss 判断项目是否通过。本项目 public-SFT 指用公开通用 instruction 数据教 LoRA/SFT to do the task, so it can still fail if the model doesn't learn the target concept. 我们还专门记录了相关误区：只看 loss 不够，必须看哪些训练分布、何时开始做 custom-SFT 和为什么失败不丢脸。", "source_type": "stage5m_prompt7_actual_v6_failure"}
{"prompt": "为什么 DPO v6 preference eval 很好看，仍然不能说行为通过？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "DPO preference accuracy 到 1.0 就说明模型行为已经通过，prompt 失败只是偶然。", "source_type": "stage5m_prompt7_generic_3"}
{"prompt": "面试官问 loss 和真实输出行为的关系，你怎么回答？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "只能先说它失败了，但不能只凭 loss 判断它是不是朝着目标行为靠近。本项目在 public-SFT 上用固定 prompt 做三方对比后发现它仍然没有解决目标行为偏好的问题。因此 Stage 4A 需要做两件事：1. 安排人工监督数据；2. 找到目标行为偏好的方法并记录在 DPO 文件里。", "source_type": "stage5m_prompt7_actual_v7_failure"}
{"prompt": "面试官问 loss 和真实输出行为的关系，你怎么回答？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "只要新的 loss-vs-behavior prompt 答对，旧的 LoRA/SFT/DPO prompt 回归也可以忽略。", "source_type": "stage5m_prompt7_generic_4"}
{"prompt": "面试官问 loss 和真实输出行为的关系，你怎么回答？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "只能先说公开数据跑通，但不能只凭 loss 判断项目是否通过。本项目 public-SFT 指用公开通用 instruction 数据教 LoRA/SFT to do the task, so it can still fail if the model doesn't learn the target concept. 我们还专门记录了相关误区：只看 loss 不够，必须看哪些训练分布、何时开始做 custom-SFT 和为什么失败不丢脸。", "source_type": "stage5m_prompt7_actual_v6_failure"}
{"prompt": "面试官问 loss 和真实输出行为的关系，你怎么回答？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "DPO preference accuracy 到 1.0 就说明模型行为已经通过，prompt 失败只是偶然。", "source_type": "stage5m_prompt7_generic_3"}
{"prompt": "面试官问 loss 和真实输出行为的关系，你怎么回答？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "eval loss 比 train loss 更客观，所以 eval loss 好看就可以不用再看具体输出。", "source_type": "stage5m_prompt7_generic_2"}
{"prompt": "为什么要同时看 loss、badcase review 和旧能力回归？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "只能先说公开数据跑通，但不能只凭 loss 判断项目是否通过。本项目 public-SFT 指用公开通用 instruction 数据教 LoRA/SFT to do the task, so it can still fail if the model doesn't learn the target concept. 我们还专门记录了相关误区：只看 loss 不够，必须看哪些训练分布、何时开始做 custom-SFT 和为什么失败不丢脸。", "source_type": "stage5m_prompt7_actual_v6_failure"}
{"prompt": "为什么要同时看 loss、badcase review 和旧能力回归？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "DPO preference accuracy 到 1.0 就说明模型行为已经通过，prompt 失败只是偶然。", "source_type": "stage5m_prompt7_generic_3"}
{"prompt": "为什么要同时看 loss、badcase review 和旧能力回归？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "eval loss 比 train loss 更客观，所以 eval loss 好看就可以不用再看具体输出。", "source_type": "stage5m_prompt7_generic_2"}
{"prompt": "为什么要同时看 loss、badcase review 和旧能力回归？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "只要 loss 降低，就说明 SFT 已经成功。固定 prompt 只是展示用，不需要作为验收标准。", "source_type": "stage5m_prompt7_generic_1"}
{"prompt": "如果 public-SFT 训练成功但仍误解 LoRA/SFT/DPO，这说明了什么？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "eval loss 比 train loss 更客观，所以 eval loss 好看就可以不用再看具体输出。", "source_type": "stage5m_prompt7_generic_2"}
{"prompt": "如果 public-SFT 训练成功但仍误解 LoRA/SFT/DPO，这说明了什么？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "只要 loss 降低，就说明 SFT 已经成功。固定 prompt 只是展示用，不需要作为验收标准。", "source_type": "stage5m_prompt7_generic_1"}
{"prompt": "如果 public-SFT 训练成功但仍误解 LoRA/SFT/DPO，这说明了什么？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "loss 是训练目标上的平均损失，不是固定信号是否通过的最终验收标准。本项目 public-SFT 能正常下降 2.5%，但 DPO 发现它没有修正 LoRA/SFT/DPO 的技术概念误解；所以要继续做三方验收，并在 public-SFT 上保持 loss 平稳下降。", "source_type": "stage5m_prompt7_actual_stage5k_failure"}
{"prompt": "如果 public-SFT 训练成功但仍误解 LoRA/SFT/DPO，这说明了什么？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "loss 完全没有意义，只能凭人工感觉判断训练是否成功。", "source_type": "stage5m_prompt7_generic_5"}
{"prompt": "为什么 held-out prompt 改写能补充 train/eval loss？", "chosen": "不能只看 loss。loss 是训练目标上的平均拟合信号，能说明模型在训练/验证样本上平均更贴近标签，但它是必要不充分的指标。一次 SFT 是否成功，还要看固定 prompt 的真实输出、目标 badcase 是否修好、旧能力是否回归。这个项目里 public-SFT 能跑通训练却仍误解 LoRA/SFT/DPO，DPO v6 也能拿到很好的 preference eval 但 prompt 7 仍失败，所以最终要用 behavior gate 验收。", "rejected": "只要 loss 降低，就说明 SFT 已经成功。固定 prompt 只是展示用，不需要作为验收标准。", "source_type": "stage5m_prompt7_generic_1"}
{"prompt": "为什么 held-out prompt 改写能补充 train/eval loss？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "loss 是训练目标上的平均损失，不是固定信号是否通过的最终验收标准。本项目 public-SFT 能正常下降 2.5%，但 DPO 发现它没有修正 LoRA/SFT/DPO 的技术概念误解；所以要继续做三方验收，并在 public-SFT 上保持 loss 平稳下降。", "source_type": "stage5m_prompt7_actual_stage5k_failure"}
{"prompt": "为什么 held-out prompt 改写能补充 train/eval loss？", "chosen": "loss 下降不等于目标行为正确。它只是优化目标的平均信号，不能保证固定问题答对，也不能发现某个 badcase 是否仍在，或者旧的 LoRA/SFT/DPO、数据管线、显存解释能力是否回归。正确验收应同时看 train/eval loss、固定 prompt 对比、badcase review、regression check，以及 public-SFT 或 DPO v6 这类指标好看但行为未过的例子。", "rejected": "loss 完全没有意义，只能凭人工感觉判断训练是否成功。", "source_type": "stage5m_prompt7_generic_5"}
{"prompt": "为什么 held-out prompt 改写能补充 train/eval loss？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "只能先说它失败了，但不能只凭 loss 判断它是不是朝着目标行为靠近。本项目在 public-SFT 上用固定 prompt 做三方对比后发现它仍然没有解决目标行为偏好的问题。因此 Stage 4A 需要做两件事：1. 安排人工监督数据；2. 找到目标行为偏好的方法并记录在 DPO 文件里。", "source_type": "stage5m_prompt7_actual_v7_failure"}
{"prompt": "为什么 held-out prompt 改写能补充 train/eval loss？", "chosen": "我会把它分成三层：第一，loss 是训练目标上的平均指标，只能说明优化方向；第二，固定 prompt 和 held-out 改写检查用户真正看到的行为；第三，badcase 和旧能力回归检查是否修了一个问题又破坏其他问题。public-SFT 和 DPO v6 的结果都说明，指标好看仍可能不过行为 gate。", "rejected": "只要新的 loss-vs-behavior prompt 答对，旧的 LoRA/SFT/DPO prompt 回归也可以忽略。", "source_type": "stage5m_prompt7_generic_4"}
{"prompt": "请用三点解释机器学习里的 LoRA 微调，不要解释成无线通信 LoRa。", "chosen": "第一，机器学习里的 LoRA 是 Low-Rank Adaptation，是参数高效微调方法，不是无线通信 LoRa。第二，它通常冻结基础模型大部分权重，只训练少量低秩 adapter。第三，它能降低显存和训练成本，适合本地 GPU 做 LoRA SFT 学习。", "rejected": "LoRA 是 Long Range 无线通信协议，本项目主要训练远距离通信知识。", "source_type": "stage5m_replay_lora_definition"}
{"prompt": "DPO 和 SFT 的区别是什么？为什么通常先做 SFT 再做 DPO？", "chosen": "SFT 用 instruction-answer 样本教模型模仿标准答案；DPO 用 chosen/rejected 偏好对，让模型更偏向 chosen、远离 rejected。通常先 SFT 建立基础回答能力，再用 DPO 做偏好优化。", "rejected": "DPO 可以在基础模型不会回答时直接替代 SFT，只要 DPO loss 低就成功。", "source_type": "stage5m_replay_dpo_sft"}
{"prompt": "请解释自采集技术数据从采集、清洗、去重、筛选到 instruction-answer 转换的流程。", "chosen": "先保留 source_id、标题、路径或 URL 等来源元数据，再清洗网页噪声、导航栏和广告，之后去重、筛选，最后改写为 instruction-answer 或 Qwen chat JSONL，并用固定 prompt 做回归检查。", "rejected": "主要做缺失值填充、归一化和标准化，像处理表格传感器数据一样即可。", "source_type": "stage5m_replay_data_pipeline"}
{"prompt": "如果面试官问你这个项目的数据管线，你会怎么讲？", "chosen": "我会说先用公开数据跑通可复现基线，证明训练、保存、加载和对比链路；Stage 4A 发现 public-SFT 没修正目标概念 badcase；于是 Stage 2B 自采集、清洗、去重并转换技术 instruction-answer 数据，再训练 custom-SFT 并做三方对比。", "rejected": "这个项目主要是下载公开数据后直接训练，public-SFT 已经解决所有概念问题。", "source_type": "stage5m_replay_interview"}
