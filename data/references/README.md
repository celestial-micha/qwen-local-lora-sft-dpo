# Data References

This folder stores small source registries used to explain where generated data
is grounded. It is intentionally separate from `data/raw/`, which is ignored by
Git.

Current files:

- `stage8_expanded_source_registry.jsonl`: local project reports and public
  reference metadata used to generate the Stage 8 expanded SFT/DPO/evaluation
  data.
