"""Train DPO.

Stage 5 placeholder.

Important: the current Windows local environment has shown python.exe native
crashes when importing some heavy training stacks in-process. The project should
first complete Stage 1-4 with base inference and LoRA SFT. DPO should be added
after the SFT path is stable.
"""


def main() -> None:
    raise SystemExit(
        "Stage 5 placeholder: DPO is intentionally deferred until local LoRA SFT is stable."
    )


if __name__ == "__main__":
    main()
